name: Build DeSmuME AppImage

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build DeSmuME AppImage
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install libfuse2 and dependencies
        run: |
          sudo apt-get update
          sudo apt-get -y install libfuse2

      - name: Install dependencies
        run: sudo apt update && sudo apt install meson libsdl2-dev libpcap-dev libgtk-3-dev libgtk2.0-dev autoconf libglu1-mesa-dev

      - name: Build DeSmuME
        run: |
          cd desmume/src/frontend/posix/
          meson build
          ninja -C build

      - name: Create AppDir directory
        run: mkdir -p AppDir
      
      - name: Copy Desktop File to AppDir
        run: cp desmume/src/frontend/posix/gtk2/desmume.desktop AppDir/

      - name: Install linuxdeploy
        run: |
          wget https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          chmod a+x linuxdeploy-x86_64.AppImage

      - name: Build AppImage
        run: |
          ./linuxdeploy-x86_64.AppImage --appdir AppDir --output appimage
          mkdir dist
          mv ./desmume*.AppImage dist/

      - name: Upload AppImage artifact
        uses: actions/upload-artifact@v2
        with:
          name: desmume-appimage
          path: ./dist/
